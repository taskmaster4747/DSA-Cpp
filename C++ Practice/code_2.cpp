//Operators

#include<iostream>
using namespace std;

int main(){
    
   cout << ( ( 3 < 1 ) || ( 3 < 5 ) ) << endl;  // OR Operator
   cout << ( ( 3 < 1 ) && ( 3 < 5 ) ) << endl;  // AND Operator
    
    return 0;
}